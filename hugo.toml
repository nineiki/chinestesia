baseURL = "https://nineiki.github.io/chinestesia/"
defaultContentLanguage = "es"
languageCode = "es-ES"
title = 'Chinestesia'
theme = 'ananke'
[caches]
  [caches.images]
    dir = ':cacheDir/images'

[params]
  custom_css = ["custom.css"]
  mainSections = ["posts", "lists"]

[markup]
  [markup.goldmark]
    [markup.goldmark.renderer]
      unsafe = true